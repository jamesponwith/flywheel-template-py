[project]
name = "flywheel-project"
version = "0.0.0"
description = "Replace with your project"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.8"]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
addopts = "-q"
